# 主题色修改指南

## 🎨 主题色配置位置

### 1. **主要主题色变量** (推荐修改这里)

**文件位置：** `src/index.css`

```css
:root {
  --accent-orange: #f97316;  /* 橙色主题色 */
  --accent-blue: #3b82f6;    /* 蓝色主题色 */
}
```

**修改方法：**
- 修改 `--accent-orange` 的值来改变橙色主题
- 修改 `--accent-blue` 的值来改变蓝色主题

**注意：** 这些变量目前定义但未在 Tailwind 中使用，组件中直接使用了 Tailwind 颜色类。

---

### 2. **Tailwind 颜色配置** (全局修改)

**文件位置：** `tailwind.config.js`

当前项目直接使用 Tailwind 的默认颜色：
- `orange-500` = `#f97316`
- `blue-500` = `#3b82f6`

**如果要全局替换，可以在 `tailwind.config.js` 中扩展颜色：**

```js
theme: {
  extend: {
    colors: {
      // 自定义主题色
      'theme-orange': '#f97316',  // 修改这里改变橙色
      'theme-blue': '#3b82f6',    // 修改这里改变蓝色
      // 或者覆盖 Tailwind 默认颜色
      orange: {
        500: '#你的橙色值',
      },
      blue: {
        500: '#你的蓝色值',
      },
    },
  },
}
```

---

### 3. **背景色和文字色**

**文件位置：** `src/index.css`

```css
body {
  @apply bg-neutral-950 text-white;  /* 深色背景，白色文字 */
}
```

**修改方法：**
- `bg-neutral-950` - 修改背景色（当前是深灰色 #0a0a0a）
- `text-white` - 修改文字颜色

---

### 4. **组件中的直接颜色使用**

以下文件中直接使用了颜色类，需要全局搜索替换：

**主要颜色使用位置：**
- `orange-500` / `orange-600` - 橙色高亮
- `blue-500` / `blue-600` - 蓝色高亮
- `bg-orange-500` - 橙色背景
- `bg-blue-500` - 蓝色背景
- `text-orange-500` - 橙色文字
- `text-blue-500` - 蓝色文字

**涉及的文件：**
- `src/app/page.tsx`
- `src/app/command-center/page.tsx`
- `src/app/agent-network/page.tsx`
- `src/app/intelligence/page.tsx`
- `src/app/operations/page.tsx`
- `src/app/systems/page.tsx`
- `src/components/Dashboard.tsx`
- `src/components/GaugeCard.tsx`

---

## 🚀 快速修改主题色

### 方法 1：修改 Tailwind 配置（推荐）

在 `tailwind.config.js` 中添加自定义颜色：

```js
theme: {
  extend: {
    colors: {
      // 主橙色
      'primary': '#f97316',  // 改为你想要的橙色
      // 主蓝色  
      'secondary': '#3b82f6', // 改为你想要的蓝色
    },
  },
}
```

然后在所有组件中将：
- `orange-500` → `primary`
- `blue-500` → `secondary`

### 方法 2：全局搜索替换

使用 VS Code 的全局搜索替换（Ctrl+Shift+H）：

1. **替换橙色：**
   - 查找：`orange-500`
   - 替换：`你的颜色类`（如 `red-500`、`purple-500` 等）

2. **替换蓝色：**
   - 查找：`blue-500`
   - 替换：`你的颜色类`

3. **替换 hover 状态：**
   - 查找：`orange-600`
   - 替换：对应的 hover 颜色

---

## 📝 当前主题色使用情况

### 橙色 (`orange-500` / `#f97316`)
- 侧边栏高亮
- 按钮背景
- RPM 卡片
- 进度条
- 警告/高优先级标签

### 蓝色 (`blue-500` / `#3b82f6`)
- Torque 卡片
- 图表线条
- 信息标签
- 链接颜色

### 背景色
- 主背景：`bg-neutral-950` (#0a0a0a)
- 卡片背景：`bg-neutral-900` (#171717)
- 边框：`border-neutral-700` (#404040)

---

## 💡 建议

如果要统一管理主题色，建议：

1. **在 `tailwind.config.js` 中定义主题色变量**
2. **使用 CSS 变量在 `index.css` 中定义**
3. **在组件中使用变量而不是硬编码颜色**

这样可以实现一键切换主题色！

